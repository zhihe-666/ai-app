# AI 中控台 — 快速部署

> 无需 Docker。有 Python + Node 即可一键启动。

---

## 一、环境要求

| 依赖 | 版本 | 下载 |
|------|------|------|
| Python | 3.10+ | https://python.org |
| Node.js | 18+ | https://nodejs.org |
| npm | 随 Node | — |

首次启动会自动装 Python/Node 依赖（约 5-10 分钟，取决于网速），后续启动秒级。

---

## 二、一键启动

### macOS / Linux

```bash
tar -xzf ai-app.tar.gz
cd ai-app
chmod +x start.sh
./start.sh
```

### Windows

解压 `ai-app.tar.gz`，双击 `start.bat`，或命令行：

```cmd
cd ai-app
start.bat
```

脚本会自动：
1. 检查 Python/Node 环境
2. 创建后端 venv + 装依赖
3. 装前端依赖 + build
4. 复制飞书配置（自带 lark-config）
5. 装 lark-cli（飞书命令行）
6. 加载默认 LLM/Git 配置（自带 .env）
7. 启动后端

---

## 三、访问

启动完成后，浏览器打开：

```
http://localhost:5000
```

同内网其他用户访问：

```
http://你的IP:5000
```

查 IP：macOS 终端 `ipconfig getifaddr en0`

---

## 四、已内置的默认配置

启动脚本自带以下配置，对方无需配置即可直接使用：

| 配置 | 位置 | 说明 |
|------|------|------|
| LLM API Key | `backend/.env` | DeepSeek API |
| LLM Base URL | `backend/.env` | https://api.deepseek.com |
| LLM Model | `backend/.env` | deepseek-v4-flash |
| Git Token | `backend/.env` | GitLab 访问令牌（algorithm-monorepo） |
| 飞书配置 | `lark-config/` | lark-cli token 配置 |

对方想用自己的配置：在浏览器页面侧边栏底部"全局配置"覆盖即可。

---

## 四-B、知识库微服务对接（可选）

中控台的"知识库问答"和"知识库管理"模块依赖知识库微服务（FastAPI，端口 8000）。
微服务有自己的 LLM 和 GitLab 配置，中控台通过 **共享 .env** 让两边用同一套配置。

### 共享配置原理

中控台 `backend/.env` 含一组微服务同名环境变量：

```env
# LLM（DeepSeek，和 DEFAULT_* 同源）
DEEPSEEK_API_KEY=sk-xxx
DEEPSEEK_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat

# GitLab（微服务同步 inference-studio 仓库，token 与中控台的 algorithm-monorepo 不同）
GL_URL=https://gitlab.shizhuang-inc.com
GL_TOKEN=xxx
GL_PROJECT_ID=ai-studio/inference-studio
GL_SYNC_REF=master
```

微服务的 `src/kb/config.py` 自动读这些环境变量，无需改微服务代码。

### 启动微服务

**单独启动（当前推荐）：**

确保 `ju`（知识库微服务）和 `ai-app`（中控台）在同一父目录下，目录结构如：

```
父目录/
├── ai-app/      # 中控台
└── ju/          # 知识库微服务
```

然后在中控台目录运行：

```bash
./start_microservice.sh
```

脚本默认找同级 `../ju`。如微服务目录名不同或位置不同，用环境变量覆盖（相对中控台目录）：

```bash
# 微服务目录名为 kb-service
KB_SERVICE_DIR=../kb-service ./start_microservice.sh
```

脚本会：
1. 加载中控台 `backend/.env`（注入 DEEPSEEK_API_KEY/GL_TOKEN 等）
2. 激活微服务 venv
3. 启动微服务（端口 8000）

**聚合启动（阶段二，待对方机器有 Chroma/ollama 环境后）：**

中控台 `start.sh` 一键启动微服务 + 中控台（后续实现）。

### 微服务依赖

微服务需要：
- Python 3.10+ + 微服务自己 venv
- ChromaDB（向量库）
- ollama + `qwen3-embedding:4b` 模型（本地 embedding）
- DeepSeek API（LLM 调用）

对方机器没有这些时，微服务启不起来，但中控台其他模块（会议TODO/迭代统计/功能变更分析/PRD）不受影响，可正常使用。

---

## 五、常见问题

### Q1: 启动报"python3: command not found"
装 Python 3.10+，macOS 可 `brew install python`。

### Q2: npm install 很慢
脚本会自动切国内镜像源。仍慢的话手动：
```bash
npm config set registry https://registry.npmmirror.com
```

### Q3: pip install 很慢
脚本会自动切清华源。仍慢的话手动：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q4: 飞书功能报错
检查 `~/.dewuclaw/lark-cli-config/cli_aa847daba1bc1bb3` 是否存在。lark-cli 配置可能过期，需重新登录：
```bash
lark-cli auth login
```

### Q5: 功能变更分析 AST 报错
code-analyzer 的 dist 已打包，无需 build。若报 module not found，进 `tools/code-analyzer` 跑 `npm install && npm run build`。

### Q6: 端口 5000 被占用
改 `backend/run.py` 第 11 行 `port=5000` 改成其他端口。

---

## 六、停止服务

终端按 `Ctrl+C` 即可停止。

---

## 七、目录说明

```
ai-app/
├── start.sh          # macOS/Linux 启动脚本
├── start.bat         # Windows 启动脚本
├── backend/          # Flask 后端
│   ├── .env          # 默认 LLM/Git 配置
│   ├── requirements.txt
│   └── ...
├── frontend/         # React 前端
│   └── dist/         # 已 build 的前端（可直接 serve）
├── tools/
│   └── code-analyzer/
│       └── dist/     # 已 build 的 AST 工具
├── lark-config/      # 飞书 lark-cli 配置
└── 部署说明.md
```
